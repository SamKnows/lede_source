--- a/service.c
+++ b/service.c
@@ -163,7 +163,7 @@ service_reply(struct interface *iface, s
 			continue;
 		if (service_domain && strcmp(s->service, service_domain))
 			continue;
-		service_reply_single(iface, to, s, ttl, 0);
+		service_reply_single(iface, to, s, ttl, 1);
 	}
 }
 
